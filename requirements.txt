# Winchell Analytics — Python 3.11 data pipeline
#
# Scraping + parsing
httpx==0.27.2          # HTTP client with HTTP/2, timeouts, retries
selectolax==0.3.21     # fast HTML parser (Modest/Lexbor) for selector work
protego==0.3.1         # robots.txt parsing / fetch permission checks

# Live sales aggregator (pipeline/livesales)
curl_cffi==0.7.3       # Chrome TLS impersonation; some sale sites block default fingerprints
requests==2.32.3       # fallback session when curl_cffi is unavailable
tenacity==9.0.0        # retried GETs with exponential backoff

# Data modelling
pydantic==2.9.2        # canonical models + validation

# Optional storage sync (S3) — imported lazily in pipeline.run --publish
# boto3==1.35.36
