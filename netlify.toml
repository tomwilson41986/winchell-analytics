[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so client-side (React Router) routes resolve on direct hits / refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
